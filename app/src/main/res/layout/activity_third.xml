<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/entrantsmanager_bg"
    tools:context=".ThirdActivity">

    <!-- Top Left Button -->

    <!-- First Input Box -->

    <!-- Second Input Box -->

    <!-- Dropdown (Spinner) -->

    <!-- Button between Dropdown and RecyclerView -->


    <ImageButton
        android:id="@+id/topLeftButton"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/backbutton"
        android:scaleType="centerCrop"
        android:background="?attr/selectableItemBackground"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="94dp"
        android:layout_marginLeft="36dp"/>


    <EditText
        android:id="@+id/firstInput"
        style="@style/CustomEditText"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_marginTop="25dp"
        android:hint="Plate No."
        android:textColor="#0A3832"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.495"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/topLeftButton"
        app:layout_constraintWidth_percent="0.75" />

    <EditText
        android:id="@+id/secondInput"
        style="@style/CustomEditText"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_marginTop="14dp"
        android:hint="Purpose"
        android:textColor="#0A3832"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.495"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/firstInput"
        app:layout_constraintWidth_percent="0.75" />

    <Spinner
        android:id="@+id/thirdInputSpinner"
        style="@style/CustomSpinner"
        android:layout_width="290dp"
        android:layout_height="50dp"
        android:layout_marginTop="98dp"
        android:foregroundTint="#0A3832"
        android:textColor="#0A3832"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.269" />

    <Button
        android:id="@+id/middleButton"
        android:layout_width="302dp"
        android:layout_height="60dp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#455900"
        android:textSize="18dp"
        android:layout_marginTop="12dp"
        android:text="Submit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/thirdInputSpinner" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="0dp"
        android:layout_height="400dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/middleButton"
        app:layout_constraintVertical_bias="1.0"
        tools:listitem="@layout/entrants_manager_item" />

</androidx.constraintlayout.widget.ConstraintLayout>
